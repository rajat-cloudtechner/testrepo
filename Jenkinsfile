pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                echo "Hello World!"
            }
        }

        stage('list files') {
            steps {
                sh "ls -ltrh"
            }
        }

    }
}
